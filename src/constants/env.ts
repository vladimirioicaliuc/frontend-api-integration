// currency code, it can be dollar, euro, or any currency
export const CURRENCY = "$";

// number of items per page when the list of products is loaded
export const PAGE_SIZE = 5;

// base url for the api
export const API_URL = "";
